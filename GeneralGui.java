import java.awt.BorderLayout;

import javax.swing.*;
import javax.swing.border.EmptyBorder;

//utilzzata per gestire input vuoto di jtextfield
public class GeneralGui extends JFrame{
    
    public GeneralGui(){
         super();
         
    }
    public void createGui(){
        // JPanel p = new JPanel(new BorderLayout());
        // p.setBorder(new EmptyBorder(10, 10, 10, 10));
        // setSize(500,200);
        // JLabel errorTitle = new JLabel("<html><h1>An error occurred </h1></html>");
        // JLabel error = new JLabel(ex.getMessage());
        // p.add(errorTitle,BorderLayout.PAGE_START);
        // p.add(error,BorderLayout.CENTER);
        // add(p);
        
    }
    
}